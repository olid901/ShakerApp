package de.diebois.shakerapp.ui;

/**
 * TODO Vielleicht wird dies hier noch rausgenommen
 * Callback, womit die UI aktualisiert wird, wenn Cocktails usw. fertig geladen sind
 */
public interface UICallback {
    void refreshView();
}
